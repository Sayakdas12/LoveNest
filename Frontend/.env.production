# Production build-time env vars (read by Vite during `npm run build`)
# These values are baked into the JS bundle — do NOT put secrets here.
VITE_API_URL=https://lovenest.in/api
VITE_SOCKET_URL=https://lovenest.in
